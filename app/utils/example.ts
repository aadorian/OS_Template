/**
 * Example utility function demonstrating TypeScript patterns
 * @param value - The input value to process
 * @returns The processed value
 */
export const exampleUtility = (value: string): string => {
  return value.trim().toLowerCase();
};


